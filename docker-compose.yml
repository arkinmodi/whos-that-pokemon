version: '3.8'
services:
  database:
    image: postgres:12.11
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
    ports: 
    - 5432:5432
    volumes:
      - db-data:/var/lib/postgresql/data

volumes:
  db-data:
